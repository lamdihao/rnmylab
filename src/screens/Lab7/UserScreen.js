import { Text, View } from "react-native"

const User = () => {
    return(
        <View style={{flex:1, alignItems: 'center', justifyContent:'center', backgroundColor: '#FFFF66'}}>
            <Text style={{color:'#006666', fontSize: 40}}>UserScreen</Text>
        </View>
    );
};

export default User;