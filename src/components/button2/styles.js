import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    textContent: {
        color: '#FFFFFF',
        textAlign: 'center',
        fontSize: 16,
        fontWeight: 'bold',
    },
    button:{
        alignItems: 'center',
        backgroughtColor: 'red',
        padding: 10,
    },
});